app: search
output_file: splunkd-health.json
mode: job
search: |
  | rest /services/server/health/splunkd
  | table title health status message
results:
  endpoint: auto
  output_mode: json
  count: 0
diagnostics:
  search_log: summary
